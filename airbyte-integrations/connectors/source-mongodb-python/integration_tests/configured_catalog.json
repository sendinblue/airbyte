{
  "streams": [
    {
      "stream": {
        "name": "testcollection",
        "json_schema": {},
        "supported_sync_modes": ["incremental"]
      },
      "sync_mode": "incremental",
      "cursor_field": ["_id"],
      "destination_sync_mode": "overwrite"
    }
  ]
}
