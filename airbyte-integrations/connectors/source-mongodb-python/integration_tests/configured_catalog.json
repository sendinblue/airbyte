{
  "streams": [
    {
      "stream": {
        "name": "exemples",
        "json_schema": {},
        "supported_sync_modes": ["incremental"]
      },
      "sync_mode": "full_refresh",
      "destination_sync_mode": "overwrite"
    }
  ]
}
